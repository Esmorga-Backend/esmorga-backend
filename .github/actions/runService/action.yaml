name: "Run Service"
description: "Run Service"
runs:
  using: "composite"
  steps:
        - name: Install dependencies
          shell: bash
          run: |
            npm ci
            npm run start &
            sleep 60
