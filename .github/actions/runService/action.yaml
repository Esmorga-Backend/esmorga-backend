name: "Run Service"
description: "Run Service"
runs:
  using: "composite"
  steps:
        - name: Install dependencies
          run: npm ci
          shell: bash
        - name: Start service
          run: npm run start &
          shell: bash
